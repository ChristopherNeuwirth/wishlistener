{
  "name": "wishlistener",
  "version": "1.0.0",
  "description": "A small tool to notifiy you about wild discounts on your amazon wishlist items.",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./src --out-dir dist/ --copy-files",
    "start": "npm run build && node dist/main.js"
  },
  "keywords": [
    "node",
    "javascript",
    "js"
  ],
  "author": "Christopher Neuwirth",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "husky": "^0.13.4",
    "nightmare": "^2.10.0",
    "rimraf": "^2.6.1"
  }
}
